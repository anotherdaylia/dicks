rule  "Mininum Packagethreshold0"
    when
        $o : Order()
        $i : Product( ((weight > 5)|| ( size < 3))&& (( productID == def234 )|| (productID == def234 )|| (productID == ghi456 ))) from $o.getProducts()
    then
        $i.minPackage();
end

