rule  "Mininum Packagethreshold0"
    when
        $o : Order()
        $i : Product( ((weight > 5)|| ( size > 3))&& (( productID ==  )|| (productID == 2 )))from $o.getProducts()
    then
        $i.minPackage();
end

