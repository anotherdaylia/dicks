rule  "Mininum Packagethreshold0"
    when
        $o : Order()
        $i : Product( ((weight > 5)|| ( size = 1)|| ( y < 3))&& (( productID == 1 )|| (productID == 2 )|| (productID == 3 ))) from $o.getProducts()
    then
        $i.minPackage();
end

